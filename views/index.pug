extends layout

block scripts
  script(src='/js/typeit.js')
  
block content
  div.hero-body.bg-img
    div.container.has-text-centered.is-vcentered
      div.columns.is-vcentered
        div.column.is-12
          h1.title.is-2 #{title}
            h2.type-it
          #sky
            .bird
              - for (var i = 0; i < 10; i++)
              .bird_body
                .bird_head
                .bird_wing_left
                  .bird_wing_left_top
                .bird_wing_right
                  .bird_wing_right_top
                .bird_tail_left
                .bird_tail_right
          br
 
